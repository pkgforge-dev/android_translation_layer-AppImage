diff --git a/libcore/luni/src/main/java/libcore/io/IoBridge.java b/libcore/luni/src/main/java/libcore/io/IoBridge.java
index 3b0513d4..938b3f5d 100644
--- a/libcore/luni/src/main/java/libcore/io/IoBridge.java
+++ b/libcore/luni/src/main/java/libcore/io/IoBridge.java
@@ -668,6 +668,15 @@ public final class IoBridge {
 
             return fd;
         } catch (ErrnoException errnoException) {
+            // the sockets are created as ipv6 dual-stack, but if the host
+            // has no ipv6 support fall back to ipv4
+            if (domain == AF_INET6 && errnoException.errno == EAFNOSUPPORT) {
+                try {
+                    return Libcore.os.socket(AF_INET, type, protocol);
+                } catch (ErrnoException errnoException2) {
+                    throw errnoException2.rethrowAsSocketException();
+                }
+            }
             throw errnoException.rethrowAsSocketException();
         }
     }
diff --git a/libcore/ojluni/src/main/native/net_util_md.c b/libcore/ojluni/src/main/native/net_util_md.c
index d15e29da..6eb9687e 100644
--- a/libcore/ojluni/src/main/native/net_util_md.c
+++ b/libcore/ojluni/src/main/native/net_util_md.c
@@ -330,16 +330,10 @@ jint  IPv6_supported()
     SOCKADDR sa;
     socklen_t sa_len = sizeof(sa);
 
-    // This one below is problematic, will fail without proper permissions
-    // and report no ipv6 for some and ipv6 for others.
-    //fd = JVM_Socket(AF_INET6, SOCK_STREAM, 0) ;
-    //if (fd < 0) {
-        /*
-         *  TODO: We really cant tell since it may be an unrelated error
-         *  for now we will assume that AF_INET6 is not available
-         */
-    //    return JNI_FALSE;
-    //}
+    fd = JVM_Socket(AF_INET6, SOCK_STREAM, 0);
+    if (fd < 0) {
+        return JNI_FALSE;
+    }
 
     /*
      * If fd 0 is a socket it means we've been launched from inetd or
@@ -358,7 +352,7 @@ jint  IPv6_supported()
      * Don't need to parse the line - we just need an indication.
      */
 #ifdef __linux__
-    /*    {
+    {
         FILE *fP = fopen("/proc/net/if_inet6", "r");
         char buf[255];
         char *bufP;
@@ -373,7 +367,7 @@ jint  IPv6_supported()
             close(fd);
             return JNI_FALSE;
         }
-        }*/
+    }
 #endif
 
     /**
@@ -428,7 +422,7 @@ jint  IPv6_supported()
 
     ipv6_fn = JVM_FindLibraryEntry(RTLD_DEFAULT, "inet_pton");
     if (ipv6_fn == NULL ) {
-        // close(fd);
+        close(fd);
         return JNI_FALSE;
     }
 
@@ -456,7 +450,7 @@ jint  IPv6_supported()
         getaddrinfo_ptr = NULL;
     }
 
-    //close(fd);
+    close(fd);
     return JNI_TRUE;
 #endif /* AF_INET6 */
 }
